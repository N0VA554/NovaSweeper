{
  "name": "novasweeper-apps",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd .. && npm install && npm run build --workspace @novasweeper/web"
  },
  "dependencies": {
    "next": "^16.2.9",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
